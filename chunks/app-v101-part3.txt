function updateMaskStats(){ const mesh=currentMesh(); if(!mesh || !mesh.geometry.userData.mask){ $('maskPill').textContent='Máscara: 0%'; return; } const mask = mesh.geometry.userData.mask; let sum=0; for(let i=0;i<mask.length;i++) sum += mask[i]>0.5 ? 1 : 0; $('maskPill').textContent=`Máscara: ${Math.round(sum/mask.length*100)}%`; }
function updateToggle(el, on){ el.classList.toggle('active', on); }
function refreshUi(){
  const part=currentPart(), mesh=currentMesh();
  $('selectionPill').textContent = part ? part.name : 'Nenhuma camada';
  const editable = !!part && part.visible && !part.locked;
  $('deleteLayerBtn').disabled = !part; $('fitBtn').disabled = !part; ['toggleSculptBtn','undoStrokeBtn','clearSculptBtn','clearMaskBtn','exportBtn','subdivideBtn','resetMeshBtn','remeshBtn','applyTransformBtn','resetTransformBtn'].forEach(id=>$(id).disabled = !editable);
  $('saveProjectBtn').disabled = !parts.length; $('duplicateLayerBtn').disabled = !part; $('duplicateLayerBtn2').disabled = !part; $('renameLayerBtn').disabled = !part; $('exportAllBtn').disabled = !parts.length;
  $('brushStatus').textContent = !part ? '⏳ Crie ou selecione uma camada' : (!part.visible ? '🚫 Camada oculta' : part.locked ? '🔒 Camada bloqueada' : '✅ Pincel pronto');
  $('sculptPill').textContent = sculptEnabled ? `Escultura: ${maskMode==='paint'?'máscara':maskMode==='erase'?'apagar máscara':'ligada'}` : 'Escultura: desligada';
  $('toggleSculptBtn').textContent = sculptEnabled ? 'Desativar escultura' : 'Ativar escultura'; $('toggleSculptBtn').className = sculptEnabled ? 'warn' : 'ok';
  updateTriCount(); updateMaskStats();
}
function updateBrushUi(){ $('sizeV').textContent=$('brushSize').value; $('strengthV').textContent=(+$('brushStrength').value).toFixed(2); $('spacingV').textContent=(+$('brushSpacing').value).toFixed(2); $('falloffV').textContent=(+$('brushFalloff').value).toFixed(2); updateToggle($('symXBtn'),symmetry.x); updateToggle($('symYBtn'),symmetry.y); updateToggle($('symZBtn'),symmetry.z); updateToggle($('maskPaintBtn'),maskMode==='paint'); updateToggle($('maskEraseBtn'),maskMode==='erase'); refreshUi(); }
function applyMaterial(){ const mesh=currentMesh(); if(mesh){ mesh.material.dispose(); mesh.material = makeMaterial(); setStatus('Material aplicado na camada selecionada.', 'ok'); } }
function setPointer(evt){ const rect=renderer.domElement.getBoundingClientRect(); pointer.x=((evt.clientX-rect.left)/rect.width)*2-1; pointer.y=-((evt.clientY-rect.top)/rect.height)*2+1; }
function intersectVisibleParts(evt){ setPointer(evt); raycaster.setFromCamera(pointer,camera); const objs=parts.filter(p=>p.visible).map(p=>p.mesh); return raycaster.intersectObjects(objs, false); }
function intersectSelected(evt){ const mesh=currentMesh(); if(!mesh || !currentPart().visible) return null; setPointer(evt); raycaster.setFromCamera(pointer,camera); return raycaster.intersectObject(mesh,false)[0] || null; }
function ensureBrushRing(){ if(brushRing) return; const geo=new THREE.RingGeometry(0.98,1.0,48); const mat=new THREE.MeshBasicMaterial({color:0x29d3ff,side:THREE.DoubleSide,transparent:true,opacity:0.9}); brushRing=new THREE.Mesh(geo,mat); brushRing.visible=false; scene.add(brushRing); }
function updateBrushRing(hit){ ensureBrushRing(); if(!hit || !currentMesh()){ brushRing.visible=false; return; } const radius=+$('brushSize').value*0.018; brushRing.visible=true; brushRing.position.copy(hit.point).add(hit.face.normal.clone().multiplyScalar(0.01)); brushRing.quaternion.setFromUnitVectors(new THREE.Vector3(0,0,1), hit.face.normal.clone().normalize()); brushRing.scale.set(radius,radius,radius); brushRing.material.color.set(maskMode==='paint'?0xffbe62:maskMode==='erase'?0x66e0a3:0x29d3ff); }
function cloneState(){ const geom=currentMesh().geometry; return {pos:geom.attributes.position.array.slice(), mask:geom.userData.mask.slice(), cols:geom.getAttribute('color').array.slice()}; }
function pushUndo(){ const mesh=currentMesh(); if(!mesh) return; sculptUndoStack.push(cloneState()); if(sculptUndoStack.length>20) sculptUndoStack.shift(); }
function undoStroke(){ const mesh=currentMesh(); if(!mesh || sculptUndoStack.length===0) return; const state=sculptUndoStack.pop(), geom=mesh.geometry; geom.attributes.position.array.set(state.pos); geom.attributes.position.needsUpdate=true; geom.userData.mask = state.mask.slice(); geom.getAttribute('color').array.set(state.cols); geom.getAttribute('color').needsUpdate=true; geom.computeVertexNormals(); updateTriCount(); updateMaskStats(); setStatus('Último stroke foi desfeito.', 'ok'); }
function clearSculpt(){ const mesh=currentMesh(); if(!mesh) return; const part=currentPart(); mesh.geometry.dispose(); mesh.geometry = mesh.userData.baseGeometry.clone(); initVertexAttributes(mesh.geometry); mesh.geometry.userData.mask = mesh.userData.baseMask.slice(); mesh.material.dispose(); mesh.material = makeMaterial(); sculptUndoStack.length=0; refreshUi(); setStatus(`Detalhes removidos de ${part.name}.`, 'ok'); }
function clearMask(){ const mesh=currentMesh(); if(!mesh) return; const geom=mesh.geometry, mask=geom.userData.mask, colors=geom.getAttribute('color').array; mask.fill(0); for(let i=0;i<mask.length;i++){ colors[i*3]=1; colors[i*3+1]=1; colors[i*3+2]=1; } geom.getAttribute('color').needsUpdate=true; updateMaskStats(); setStatus('Máscara removida.', 'ok'); }
function resetMesh(){ const mesh=currentMesh(); if(!mesh) return; mesh.geometry.dispose(); mesh.geometry = mesh.userData.baseGeometry.clone(); initVertexAttributes(mesh.geometry); mesh.geometry.userData.mask = new Float32Array(mesh.geometry.attributes.position.count); mesh.userData.baseMask = new Float32Array(mesh.geometry.attributes.position.count); sculptUndoStack.length=0; refreshUi(); setStatus('Malha resetada ao bruto original.', 'ok'); }
function subdivideSelected(){ const mesh=currentMesh(); if(!mesh) return; const old=mesh.geometry, pos=old.attributes.position.array, mask=old.userData.mask, cols=old.getAttribute('color').array; const newVerts=[], newMask=[], newCols=[]; const V=i=>new THREE.Vector3(pos[i],pos[i+1],pos[i+2]);
